name: Secret Masking Test
on:
  workflow_dispatch:

jobs:
  test-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Echo secret
        run: echo "Token is: ${{ secrets.MY_SENSITIVE_TOKEN }}"
