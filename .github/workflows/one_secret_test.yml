name: Secret Masking Test

on:
  workflow_dispatch:

jobs:
  test-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Echo secret
        env:
          TOKEN: ${{ secrets.MY_SENSITIVE_TOKEN }}
        run: echo "Token is $TOKEN"
